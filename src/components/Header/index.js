import React from'react';

const Header = props => {
    return (
        <header className="header">
         <nav className="HeaderMenu">
             <a href="#">Home</a>
             <a href="#">About us</a>
             <a href="#">Contact us</a>
         </nav>
         <div>
             social Media Icons
         </div>
        </header>
    );
}

export default Header;

